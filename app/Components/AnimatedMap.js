// app/Components/MapWrapper.js
// app/Components/MapWrapper.js
// 'use client';

// import dynamic from 'next/dynamic';
// import { useState } from 'react';
// import MapPage from '../LeafletMap';

// // Dynamically import LeafletMap without SSR
// const LeafletMap = dynamic(() => import('../LeafletMap'), { ssr: false });

// export default function MapWrapper({ startTime, endTime, status }) {
//   return <MapPage time={{ startTime, endTime }} status={status} />;
}